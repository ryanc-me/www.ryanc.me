{% macro tag_flag(tag) %}
    <span class="--tag-flag" style="background-color: {{ tag.accent_color }}">
        <span class="tag-name" href="{{ tag.url }}">{{ tag.slug }}</span>
    </span>
{% endmacro %}